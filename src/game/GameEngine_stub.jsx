import React from 'react';

export default React.memo(function GameEngineStub() {
  // Minimal stub while we fix the full GameEngine implementation.
  return (
    <div style={{ width: '100%', height: '100vh' }} />
  );
});
